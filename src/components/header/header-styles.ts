import styled from "styled-components";

export const StyledHeader = styled.header`
    display: flex;
    justify-content: center;
    padding: 1em;
    width: 90vw;
    margin: 0 auto;

    font-weight: 700;
    font-size: 2rem;
`;